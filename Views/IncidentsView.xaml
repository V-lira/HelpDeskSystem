<UserControl x:Class="HelpDeskSystem.Views.IncidentsView"

             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0 0 0 20">

            <TextBlock Text="Инциденты" FontSize="32" FontWeight="Bold" DockPanel.Dock="Left"/>
            <Button Content="➕ Создать инцидент" Width="220" Height="45" HorizontalAlignment="Right" Style="{StaticResource MaterialDesignRaisedButton}" Click="CreateIncidentButton_Click"/>
        </DockPanel>
        <!-- Search -->
        <TextBox x:Name="SearchTextBox" Grid.Row="1" Width="300" Height="40" Margin="0 0 0 20" TextChanged="SearchTextBox_TextChanged" materialDesign:HintAssist.Hint="Поиск инцидентов"/>
        <!-- Table -->
        <materialDesign:Card Grid.Row="2" Padding="20">
            <DataGrid x:Name="IncidentsGrid" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" MouseDoubleClick="IncidentsGrid_MouseDoubleClick">
                <DataGrid.Columns>
                    <!-- Number -->
                    <DataGridTextColumn Header="Номер" Binding="{Binding Number}" Width="140"/>
                    <!-- Title -->
                    <DataGridTextColumn Header="Тема" Binding="{Binding Title}" Width="*"/>
                    <!-- Priority -->
                    <DataGridTemplateColumn Header="Приоритет" Width="140">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Background="{Binding Priority, Converter={StaticResource PriorityToBrushConverter}}" CornerRadius="10" Padding="10 4" HorizontalAlignment="Center">
                                    <TextBlock Text="{Binding Priority}" Foreground="White" FontWeight="SemiBold"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <!-- Status -->
                    <DataGridTemplateColumn Header="Статус" Width="160">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Background="{Binding Status, Converter={StaticResource StatusToBrushConverter}}" CornerRadius="10" Padding="10 4" HorizontalAlignment="Center">
                                    <TextBlock Text="{Binding Status}" Foreground="White" FontWeight="SemiBold"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <!-- assigned -->
                    <DataGridTextColumn Header="Исполнитель" Binding="{Binding AssignedUser}" Width="160"/>
                    <!-- SLA -->
                    <DataGridTextColumn Header="SLA" Binding="{Binding SLA}" Width="140"/>
                    <!-- created -->
                    <DataGridTextColumn Header="Создан" Binding="{Binding CreatedAt}" Width="180"/>
                    <!-- due -->
                    <DataGridTextColumn Header="Deadline" Binding="{Binding DueDate}" Width="180"/>
                    <!-- actions -->
                    <DataGridTemplateColumn Header="Действия" Width="220">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <!-- Edit -->
                                    <Button Content="✏️" Width="40" Height="35" Margin="0 0 10 0" Click="EditIncidentButton_Click" Tag="{Binding Id}" Style="{StaticResource MaterialDesignOutlinedButton}"/>
                                    <!-- Delete -->
                                    <Button Content="🗑" Width="40" Height="35" Click="DeleteIncidentButton_Click" Tag="{Binding Id}" Style="{StaticResource MaterialDesignOutlinedButton}"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </materialDesign:Card>
    </Grid>
</UserControl>