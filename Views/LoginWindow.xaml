<Window x:Class="HelpDeskSystem.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Авторизация"
        Height="450"
        Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesignPaper}">

    <Grid>

        <materialDesign:Card Width="350" Height="320" Padding="30" VerticalAlignment="Center" HorizontalAlignment="Center">
            <StackPanel>
                <TextBlock Text="HelpDesk Login" FontSize="28" FontWeight="Bold" Margin="0 0 0 30" HorizontalAlignment="Center"/>
                <!-- name -->
                <TextBlock Text="ФИО" FontWeight="SemiBold" Margin="0 0 0 5"/>
                <TextBox x:Name="FullNameTextBox" Margin="0 0 0 15"/>
                <!-- role -->
                <TextBlock Text="Роль" FontWeight="SemiBold" Margin="0 0 0 5"/>
                <ComboBox x:Name="RoleComboBox" Margin="0 0 0 25">
                    <ComboBoxItem Content="Admin"/>
                    <ComboBoxItem Content="Support"/>
                </ComboBox>
                <!-- login -->
                <Button Content="🔐 Войти" Height="45" Click="LoginButton_Click" Style="{StaticResource MaterialDesignRaisedButton}"/>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</Window>