<Window x:Class="HelpDeskSystem.MainWindow"
        
        xmlns:views="clr-namespace:HelpDeskSystem.Views"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="HelpDeskSystem"
        Height="800"
        Width="1400"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        Background="{DynamicResource MaterialDesignPaper}">
        <!--Background="#F5F5F5"-->

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <!--<Border Background="White"-->
        <Border Background="{DynamicResource MaterialDesignCardBackground}"
                Grid.Column="0">

            <StackPanel>
                <!-- LOOGOO -->
                <TextBlock Text="HelpDeskSystem" FontSize="28" FontWeight="Bold" Margin="20" Foreground="#512DA8"/>
                <!-- MEEENUUU -->
                <Button Content="🏠 Главная" Height="50" Margin="10" Click="DashboardButton_Click" Style="{StaticResource SidebarButtonStyle}"/>
                <Button Content="📋 Инциденты" Height="50" Style="{StaticResource SidebarButtonStyle}" Margin="10" Click="IncidentsButton_Click"/>
                <Button Content="➕ Создать" Height="50" Style="{StaticResource SidebarButtonStyle}" Margin="10" Click="CreateIncidentButton_Click"/>
                <Button Content="📊 Отчёты" Height="50" Style="{StaticResource SidebarButtonStyle}" Margin="10" Click="ReportsButton_Click"/>
                <Button Content="👥 Пользователи" Height="50" Style="{StaticResource SidebarButtonStyle}" Margin="10" Click="UsersButton_Click"/>
                <Button Content="⚙ Настройки" Height="50" Style="{StaticResource SidebarButtonStyle}" Margin="10" Click="SettingsButton_Click"/>
            </StackPanel>
        </Border>
        <!-- MAIN COONTEBNT!~!!! -->
        <Grid Grid.Column="1" Margin="20" Background="{DynamicResource MaterialDesignPaper}">
            <ContentControl x:Name="MainContent"/>
        </Grid>
    </Grid>
</Window>